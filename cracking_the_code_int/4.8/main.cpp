#include <iostream>
using namespace std;

struct node{
   node *left, *right;
   int data;
};

int traverse() {
}

int main() {

   // FIXME
   // Doing this manually royally sucks!
   // TODO
   // Add an id to each node (in addition to the data)
   // then search node by id and allow inserting new nodes
   // so that a binary tree (not BST) can be programmatically
   // created

   // leafs
   /*
   node a = {nullptr, nullptr, 5};
   node b = {nullptr, nullptr, -1};
   node c = {nullptr, nullptr, -1};
   node d = {nullptr, nullptr, 4};

   node f = {nullptr, nillptr}
   node e = {&a, &f, 2};
   node n4 = {nullptr, &n4, 1};


   node *root = &n2;
   */

   return 0;
}